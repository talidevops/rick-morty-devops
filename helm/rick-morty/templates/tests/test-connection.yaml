apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "rick-morty.fullname" . }}-test-connection"
  labels:
    {{- include "rick-morty.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "rick-morty.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
